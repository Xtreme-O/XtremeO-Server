# Database configuration example for your application
# --------------------------------------------------
# INSTRUCTIONS:
# 1. Copy this file content from line 13 and create a 'application.properties' file.
# 2. Replace the placeholder values with your actual database credentials:
#    - db.url: Replace 'your_database_name' with your database name usually "xtremo".
#    - db.username: Your MySQL username.
#    - db.password: Your MySQL password.
#
# 3. Keep 'application.properties' in .gitignore to avoid committing credentials.
# 4. Ensure your MySQL database is running and accessible from your application.

db.url=jdbc:mysql://localhost:3306/your_database_name?serverTimezone=GMT%2B2
db.username=your_username
db.password=your_password